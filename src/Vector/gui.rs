#![allow(unused_imports)]

use crate::vector;

use iced::{
    Settings,
    Sandbox,

    widget::{
        self,

        Button, button,
        Text, text,
        TextInput, text_input,

        Row,
        Column
    }
};